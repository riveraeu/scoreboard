# Scoreboard — Project Guide for Claude

## Workflow for New Features and Debugging

1. **Check memory and CLAUDE.md** — Read `MEMORY.md` and relevant memory files. Scan CLAUDE.md for the area being changed; load `docs/MODEL.md`, `docs/INFRA.md`, or `docs/FRONTEND.md` if the change touches those areas.
2. **Plan and get approval** — Present the full plan as text only (files to change, logic, edge cases). Wait for explicit user approval before editing any files.
3. **Implement** — Make the changes. If backend logic changed, confirm with `/api/tonight?debug=1` (or relevant endpoint) and print key fields proving the change is correct.
4. **Deploy and document** — `git push origin main` to deploy. Update CLAUDE.md (and the relevant `docs/*.md`) in the same commit. Save a memory entry for anything non-obvious future sessions should know.

---

## What This Is
Sports prop betting dashboard that pulls Kalshi prediction market prices, computes a model True%, and shows qualified plays with edge over the market. Vercel Edge runtime (Web Fetch + KV/Redis only — no Node APIs).

**Production**: `https://scoreboard-ivory-xi.vercel.app`
**Universal qualification**: Kalshi 67–91% · Edge ≥ 5% (client) / ≥ 3% (server, kept loose for calibration data). Game/team totals gate UNDERs by the same `noKalshiPct ∈ [67, 91]` window. SimScore is display-only since v1 was dropped 2026-05-26. Tunables live in **one source** at `api/lib/config.js` (`KALSHI_GATE` 67, `KALSHI_CAP` 91, `EDGE_GATE_SERVER` 3, `EDGE_GATE_CLIENT` 5); both server (tonight.js) and client (App.jsx, LineupsPage.jsx, TotalsBarChart.jsx) import from there. All client surfaces gate display at EDGE_GATE_CLIENT (5).

---

## Where to look

| Topic | File |
|---|---|
| Per-sport modeling internals (SimScore tiers, lambdas, miscAdj, gates, Kalshi parsing details, dedup logic) | `docs/MODEL.md` |
| Cache keys + TTLs, Upstash storage, env vars, deployment, testing, data sources | `docs/INFRA.md` |
| URL routing, App.jsx state shape, ReportPage internals (Market Report + calibration Results), live tracking mechanics, sizing, color doctrine + per-play-type explanation table | `docs/FRONTEND.md` |
| Common debugging recipes | `docs/DEBUGGING.md` |

The cross-cutting gotchas at the bottom of this file are kept inline because they bite during *any* change, not just modeling work.

---

## Architecture

### API: `api/[...path].js` (router) + `api/lib/handlers/*.js` (routes) + `api/lib/*.js` (sport modules)
Single Vercel Edge Function. `api/[...path].js` is now a ~140-line thin router: it handles CORS preflight, calls `makeCache`, and dispatches to one handler per route family. Each handler returns a `Response` on path match or `null` to fall through. The route handlers live in `api/lib/handlers/` (Phase A route split, 2026-05-26):
- `api/lib/handlers/auth.js` — `/api/auth/*` (register, login, reset, calibration, list-users, debug-redis, clear-kalshi-stale, user/picks)
- `api/lib/handlers/player.js` — `/api/player`, `/api/gamelog`, `/api/headshot`
- `api/lib/handlers/sports.js` — `/api/team`, `/api/live`
- `api/lib/handlers/dvp.js` — `/api/dvp`, `/api/nba-depth`, `/api/dvp/debug-dc`
- `api/lib/handlers/kalshi.js` — `/api/kalshi`, `/api/kalshi-snapshot`, `/api/keepalive`, `/api/kalshi-order`
- `api/lib/handlers/tonight.js` — `/api/tonight` (the play-generation pipeline, ~1857 lines after Phase B6, 2026-05-29). Remaining: Kalshi parse loop, byteam hydration, data-prep fetches, emit calls, post-emit passes, response assembly. Owns `KALSHI_GATE`/`KALSHI_CAP`/`EDGE_GATE`, `PROD_SPORTS`, `B2B_*` constants, `_isB2B` helper. `TEAM_NORM`/`normTeam`/`parseGameTeams` moved to `api/lib/tonight/parse-teams.js`; `NHL_ABBR_MAP` exported from `api/lib/nhl.js`; ML/spread/F5/halves emission moved to `api/lib/tonight/ml-spread.js`; pure helpers (nhlSoftTeams, mlbSoftTeams, glCacheKey, _parseWind, _extractMlbWeather, injury helpers) hoisted to module scope within tonight.js.

The sport/utility modules under `api/lib/`:
- `api/lib/simulate.js` — park factors + simulation functions (`log5K`, `simulateKsDist`, `buildNbaStatDist`, `simulateHits`, `simulateMLBTotalDist/NBATotalDist/NHLTotalDist`, `simulateTeamTotalDist`, `simulateTeamPtsDist`, `kDistPct/nbaDistPct/totalDistPct`, kelly/EV math), `TTO_DECAY_FACTOR`, `UMPIRE_KFACTOR`
- `api/lib/mlb.js` — orchestrator + injuries after Phase C split (2026-05-29). Keeps `buildMlbByteam` (the byteam:mlb hydration pipeline) + `buildMlbInjuryReport`, and **re-exports** `MLB_ID_TO_ABBR` / `buildLineupKPct` / `buildBarrelPct` / `buildPitcherKPct` so `from "../mlb.js"` imports work unchanged. The builders now live in:
  - `api/lib/mlb-shared.js` — `MLB_ID_TO_ABBR`, `_fs` (leaf module, no import cycle).
  - `api/lib/mlb-hitters.js` — `buildLineupKPct` (returns `batterSplitBA`, `batterHRRSplits` fields), `buildBarrelPct`.
  - `api/lib/mlb-pitchers.js` — `buildPitcherKPct` (returns `pitcherRecentKPct`, `pitcherLastStartDate`, `pitcherLastStartPC`, `pitcherInfoByTeam`, `pitcherAvgBF`, `pitcherStdBF`, `umpireByGame`, `pitcherSplitsByTeam`, etc.). Owns the 2-step `_regressedRate` + vs-L/vs-R split math. Pitcher gamelog batch uses `Promise.allSettled`.
- `api/lib/nba.js` — `buildNbaDvpStage1/FromBettingPros/Stage3FG`, `buildNbaDepthChartPos`, `buildNbaPaceData`, `buildNbaPlayerPosFromSleeper`, `warmPlayerInfoCache`, `buildNbaUsageRate`, `buildNbaInjuryReport`
- `api/lib/wnba.js` — `buildWnbaPaceData`, `buildWnbaUsageRate`, `buildWnbaInjuryReport`, `buildWnbaDvp` (server-side stat-allowed aggregate — BettingPros has no WNBA page), `WNBA_TEAM_IDS` (15 hardcoded; ESPN `/teams` list endpoint is broken for WNBA), `WNBA_ESPN_TO_CANON`/`WNBA_CANON_TO_ESPN` (CONNECTICU↔CONN, DALLAS↔DAL). WNBA model anchors on 2025 season data.
- `api/lib/nhl.js` — `buildNhlGoalieData` returns `{ goalieByTeam: {abbr: {starterName, starterId, starterSV, starterGS, source: "starter"}}, leagueAvgSV }` from one `stats/rest/en/goalie/summary` call (~77 rows, filtered to ≥5 GS). Cached `nhl:goaliepool:20252026` 6h. Primary goalie per team = max gamesStarted; team-leading starter is true ~75% of nights in RS, ~90% in playoffs. Used by NHL game-total lambda to override team-GAA fallback. NHL stats abbrs match canonical (TBL/NJD/LAK/SJS) — no translation needed. Also exports `buildNhlInjuryReport` (ESPN injury endpoint → `Map<abbr, [{name, id, status: "out"|"gtd"}]>`, cache `nhl:injuries:{date}` 30min) used by the MatchupCard injury badge; mirror of `buildNbaInjuryReport` including the `links[].href` ID parse (ESPN omits `athlete.id`). **Also exports `NHL_ABBR_MAP`** (teamId → abbr; moved from tonight.js Phase B, 2026-05-29).
- `api/lib/tonight/parse-teams.js` — `TEAM_NORM`, `normTeam`, `_VALID_TEAMS`, `parseGameTeams`. Extracted from tonight.js Phase B (2026-05-29). Kalshi-ticker → canonical-abbr maps and the ticker-segment parser.
- `api/lib/tonight/props.js` — `emitPropPlays(ctx)`. Extracted from tonight.js Phase B6 (2026-05-29). Player prop play generation for MLB-K, MLB-HRR, NBA, WNBA, NHL (~1667 LOC). Returns `{ plays, dropped, nbaDropped }`. Context: 15 props including loopMarkets, playerInfoMap, playerGamelogs, STAT_SOFT, sportByteam, injury/usage maps, DVP data.
- `api/lib/tonight/game-totals.js` — `emitGameTotalPlays(ctx)`. Extracted from tonight.js Phase B5 (2026-05-29). Game total + team total loops for all sports (~1225 LOC). Returns `{ _mlbMlContext, _nbaMlContext, _wnbaMlContext, _nhlMlContext }` consumed by ml-spread.js. Also owns B2B constants, `_isB2B`, and all injury OffRtg helpers (only used here).
- `api/lib/tonight/ml-spread.js` — `emitAllMlAndSpread(ctx)`. Extracted from tonight.js Phase B (2026-05-29). All ML / spread / F5 / halves emission blocks (MLB + NBA + WNBA + NHL), ~1231 LOC. Takes a context object with plays/dropped arrays plus the `_*MlContext` maps and market arrays.
- `api/lib/utils.js` — CORS helpers, `parseGameOdds` (returns `{total, moneyline, spread}`), `parseGameScores` (returns `{state, detail, homeScore, awayScore, gameDate, gameTime, seriesSummary}` keyed by home abbr; `seriesSummary` non-null in NBA/NHL playoffs), team rank helpers (`buildSoftTeamAbbrs`, `buildHardTeamAbbrs`, `buildTeamRankMap`)

### Frontend: Vite + React (`src/`)
Entry: `index.html` → `src/main.jsx` → `src/App.jsx`. Vercel runs `npm run build` → `dist/` on push.

- `src/App.jsx` — top-level state, routing, data fetching, player card
- `src/lib/constants.js` — `TEAM_DB`, `TOTAL_THRESHOLDS`, `MOCK_PLAYS`, `GAMELOG_COLS`, sport/stat metadata
- `src/lib/utils.js` — `slugify`, `teamUrl`, `logoUrl(sport, abbr)` (handles ESPN CDN abbr mismatches NHL `tbl→tb, njd→nj, lak→la, sjs→sj`; NBA `kat→atl`)
- `src/lib/liveStats.js` — live pick tracking helpers
- `src/lib/hooks.js` — `useIsMobile(threshold=600)`: resize+orientation-aware boolean. Use this for responsive layouts (e.g. `LineupsPage` toolbar wraps to 2 rows on mobile). `SimBadge`/`DayBar` tooltips also support tap-to-pin so SimScore breakdowns are accessible on touch devices.
- `src/components/` — `LineupsPage` (homepage tab layout), `MatchupCard` (per-game card; MLB renders a starting-pitcher row + game total/ML center column, NBA/WNBA/NHL render a "top player" row with the scoreboard Rating/Points leader and their stats), `PlaysColumn` (per-sport explanation branches: MLB-K, MLB-hitter, NBA, WNBA, NHL + generic fallback; WNBA mirrors NBA with retuned tiers and reads `play.wnba*` fields), `MyPicksColumn`, `ReportPage` (Market Report + calibration Results tabs at `/model`, driven by a sport + play-type selector — see `docs/FRONTEND.md`), `TeamPage`, `TotalsBarChart`, `DayBar`, `AddPickModal`

**Dev proxy**: `vite.config.js` proxies `/api` to production so `npm run dev` works without local backend.

See `docs/INFRA.md` for storage and cache details.

---

## Routes
- `/api/tonight` — main play generation. `?debug=1` returns dropped/preDropped + debug fields. `?bust=1` bypasses caches. **Frontend no longer busts on mount** (changed 2026-05-21): initial fetch is plain `/api/tonight`, served warm from the snap-first path in a few hundred ms. The `App.jsx` mount useEffect (~line 264) also starts a 2-min polling loop (`POLL_MS = 120_000`) that re-fetches without bust to surface newly-qualifying plays as Kalshi prices shift — aligned to the `/api/kalshi-snapshot` cron's 2min cadence so most polls land on a fresh snap. Polling pauses on `document.visibilityState !== "visible"` and fires an immediate catch-up fetch on visibility resume. Only the manual ↻ button (`bustCache()`) still uses `?bust=1` — the escape hatch for forcing a full upstream refresh (Kalshi REST, byteam, gamelogs) when a freshly-posted lineup hasn't propagated through the 10-min byteam TTL yet. The previous client-side sessionStorage cache (`tonight_v1_${date}`, 2-min TTL) stays removed — server-side snap-first is the cache layer now.
- `/api/kalshi` — raw Kalshi market data
- `/api/player`, `/api/gamelog` — ESPN player info + gamelog
- `/api/team` — team page data (gameLog, lineup, season stats, nextGame)
- `/api/live` — in-game boxscore for pick tracking (`?games=mlb:LAD:SD,nba:GSW:LAL`); player props poll this; total/team-total picks resolve from existing `meta.gameScores` (no extra fetch)
- `/api/dvp`, `/api/nba-depth`, `/api/dvp/debug-dc` — DVP/depth chart. Branches: `basketball/nba`, `basketball/wnba` (returns `position`, `rankMaps`/`softTeams`/`hardTeams` for points/rebounds/assists/threePointers from `byteam:wnba`; canonical aliases added via `WNBA_ESPN_TO_CANON` so lookups by ticker abbr resolve), `football/nfl`, `hockey/nhl`, `baseball/mlb`.
- `/api/auth/{register,login,reset,list-users,debug-redis,calibration,clear-kalshi-stale}` — auth + admin. Password min 8 chars. Admin endpoints fail-closed if `ADMIN_KEY` missing.
- `/api/auth/clear-kalshi-stale` — `POST ?ticker=KXMLBTEAMTOTAL` with `Authorization: Bearer <ADMIN_KEY>`. Deletes `kalshi:stale:{ticker}` so the next cold bundle build attempts Kalshi fresh instead of serving the stale entry. Use when a series has been rate-limit-stuck on stale data past the 30-min TTL window. Ticker validated against `/^KX[A-Z0-9]+$/`. Does **not** affect `kalshi:snap:{ticker}` — those refresh on the 2-min cron.
- `/api/kalshi-order` — `POST`, authenticated (cookie `sb_token` first, then `Authorization: Bearer`). Places a limit buy order on Kalshi's trading API. Body: `{ ticker, side: "yes"|"no", price: int (1–99 cents), count: int, clientOrderId? }`. Signs via **RSA-PSS / SHA-256 / saltLength=32** (PSS.DIGEST_LENGTH for SHA-256) using `KALSHI_API_KEY_ID` + `KALSHI_PRIVATE_KEY` env vars (PEM — supports both PKCS#1 `BEGIN RSA PRIVATE KEY` and PKCS#8; `_pkcs1ToPkcs8()` wraps PKCS#1 in a PKCS#8 ASN.1 container for Web Crypto). **Signature message: `timestamp_ms + "POST" + "/trade-api/v2/portfolio/orders"` — timestamp in milliseconds, body NOT included.** (Confirmed from kalshi-python 2.1.4 SDK source.) Every qualifying play now carries `kalshiTicker` (specific market ticker) and `kalshiSide` ("yes"/"no") in the `/api/tonight` response. Frontend shows a "Place on Kalshi" toggle in the track-play modal when the play has a ticker and the user is logged in; button becomes "Track + Place" which fires the order then tracks the pick with a 1.2s delay.
- `/api/kalshi-snapshot` — cron-only (`*/2 * * * *` in vercel.json). Fetches all 26 Kalshi series tickers (19 SERIES_CONFIG + game totals + KXMLBGAME + KXNBAGAME) and writes per-ticker `kalshi:snap:{ticker}` keys via Upstash pipeline. Bearer-auth: `CRON_SECRET`. Returns `{ok, successCount, failedCount, failed[], durationMs}`. **Hardcoded ticker list MUST stay in sync** with `SERIES_CONFIG` inside `/api/tonight` — add new series to both places.
- `/api/auth/calibration` — outcome stats. Auth: bearer JWT (any user) or `?adminKey=`. Returns `overall`, `byCategory`, `byCategoryDetail` (per-category truePct buckets, used by the `CalibModule` per ReportPage Model tab), `kStrikeouts` (K-feature breakdowns).
- `/api/user/picks` — GET/POST user picks (bearer JWT). POST accepts two body shapes: legacy `{picks: [...all], bankroll}` (full overwrite, kept for old cached clients) and delta `{upserts: [pick,...], deletes: [id,...], bankroll}` (read-modify-write merge). Client always sends delta. Initial load seeds `lastSyncedPicks` ref on the frontend so the first save after mount is a true diff. See `savePicks` in `src/App.jsx`.
- `/api/keepalive` — daily cron

---

## Models (summary)
See `docs/MODEL.md` for SimScore tiers, lambda formulas, gates, dedup, and Kalshi parsing details.

- **MLB Strikeouts** — `simulateKsDist` Monte Carlo (10k/5k), shared distribution per `team|hand` for monotonicity. Pre-sim: A1 recent form, A2 rest, E3a umpire, K% regression, E3b expectedBF blended 2026/2025. SimScore: CSW%, lineup oK%, blended hit rate (starts-only), H2H Hand, O/U.
- **MLB Hitters (HRR)** — logit-sigmoid base-rate adjustment on `(primaryPct + softPct)/2` with park, OPS, WHIP, and barrel% shifts. **PA-aware adjustment (retry 2026-05-25)**: `seasonPct` and `softPct` inverted from per-game to per-PA using **per-hitter typical PA** (from season GP, requires GP ≥ 20), then re-composed at tonight's actual PAs from `hitterPaFromSpot`. Adjustment gated to fire only when `|tonight − typical| ≥ 0.3 PAs` so hitters at their normal spot stay unadjusted. **Barrel% (added 2026-05-25)**: weight 0.25 in the logit (`hitterBarrelPct / 8.5` clamped [0.92, 1.10]). BvP softPct shrinkage (N=20) operates on PA-adjusted values. Gates: lineup spot 1–5.
- **NBA player props** — `buildNbaStatDist`, `miscAdj = injuryBoost × blowoutRisk × homeAwaySplit`, paceFactor = `(homePace × oppPace) / leagueAvg²` clamped [0.92, 1.08] (since 2026-05-25, replaces the prior linear-delta × 0.002 capped at ±3%). **Recency-weighted meanRecent + playoff-aware Bayesian shrinkage + seasonRate blend (all since 2026-05-26)**: mean uses exp-decay weighting within the last-10 slice (`HALF_LIFE=5`: most-recent weight 1.0, 5 games back 0.50, 10 back 0.25). When tonight is `seasonType === 3` AND player has ≥5 playoff games, blend playoff-weighted-mean with mixed-weighted-mean via `w = n_playoff / (n_playoff + 10)`. Std/variance still come from the full sample. Per-event `isPlayoff` flag parsed from ESPN gamelog `seasonTypes[].displayName`. Final post-sim `truePct = (1-w) × simPct + w × refRate` where `refRate = (seasonPct + softPct) / 2` and `w = min(1, sample/20) × capWeight`. capWeight = **0.5** for NBA/WNBA/NHL props + MLB-K (sim drift correction); **0.25** for MLB HRR (sanity check against multiplicative stack of park × OPS × WHIP × barrel; HRR is already rate-based at its core). SimScore: USG% (or AvgMin for REB), DVP ratio, Season HR, Soft/Tier HR, game O/U.
- **WNBA player props** — same engine as NBA (incl. paceFactor since 2026-05-25, playoff-aware lambda since 2026-05-26), retuned tiers (USG 27/22, MIN 27/22, game O/U 168/158). 2025 anchor.
- **NHL Points** — same engine, playoff-aware lambda since 2026-05-26. SimScore: TOI, GAA rank, Season HR, DVP HR, O/U.
- **NFL** — passing/rushing/receiving yards, receptions, completions, attempts. Gate: opp in soft teams.
- **Game Totals** — NegBin MC for MLB (since 2026-05-24, league-wide pooled `r` from `_gtScheduleMap` per request), Poisson MC for NHL, Normal for NBA/WNBA; sample-weighted blend with seasonHitRate. Fixes prior Poisson tail-thinness on MLB alt thresholds (var/mean ratio ~2× empirical). `_simData.mlbDispR` records the fit per pick. **MLB λ also includes:** (a) regime-aware blend (since 2026-05-25, 14-day half-life via `_MLB_HALF_LIFE_DAYS`); (b) TTO penalty on starter ERA+FIP when `expectedBF > 22` (ramps 1.0→1.10 at BF=29); (c) pitcher vs-L/vs-R split modifiers (since 2026-05-25) — `buildPitcherKPct` returns `pitcherSplitsByTeam[abbr] = { vlFipMod, vrFipMod, vlWhipMod, vrWhipMod }` shrunk toward overall with `PRIOR_IP=20`; lineup-weighted by `lineupHandByTeam[oppTeam] = { l, r, s }` (switch hitters resolve opposite starter hand); effective FIP/WHIP feed `_starterMult` instead of overall; (d) **starter days-rest bump (since 2026-05-25)**: 3-day rest → ×1.08, 4-day → ×1.04, 5+ → ×1.0. Stacked multiplicatively with TTO on FIP+ERA inputs. `_simData` records `homeOppLFrac` / `homeFipMod` / `homeFipEff` / `homeWhipEff` (and away counterparts). **NHL λ includes PP/PK adjustment (since 2026-05-25)** — net-ST factor `clamp(1 + ((ownPP − lgPP) + (lgPK − oppPK)) × 0.20, [0.90, 1.10])` from `buildNhlSpecialTeams`; data on `sportByteam.nhl.specialTeams.byTeam[abbr]`. Filter `trackedAt < 2026-05-25` from MLB total/ML/spread and NHL total/ML/spread calibration for the 2026-05-25 changes (additional to the existing 2026-05-24 filter for the NegBin swap).
- **Team Totals (MLB, NBA only)** — MLB Poisson with 50/50 ttSeasonHitRate blend; NBA Normal with sample-weighted blend. **MLB also blends recency-weighted recent runs into λ (since 2026-05-27, 14d half-life via `_MLB_HALF_LIFE_DAYS`, cap 0.50 share / denom 12 — half the game-total cap because single-team variance is larger than two-team-combined).** Stacks before `ttSeasonHitRate` blend, which still clamps within `_TT_IMPLIED_CAP.mlb = 1.5` of the regime-adjusted λ. Filter `trackedAt < 2026-05-27` from MLB teamTotal calibration.
- **MLB Moneyline (v1, 2026-05-18)** — Joint Poisson sim (`simulateMLBJoint`) from the same per-team lambdas as MLB game totals; `mlPctFromJoint` counts `home > away` over non-tie sims (ties dropped, since MLB resolves in extras). Per-side `kalshiPct` = own `yes_ask_dollars` × 100 (each ML side is its own Kalshi market, so the totals UNDER `no_ask` workaround doesn't apply). No SimScore; `dcQualified + edge ≥ 5` is the gate. Lambdas + simData cached per game in `_mlbMlContext` inside the MLB total loop; emission runs after team-total cross-dedup. `stat: "ml"` so MarketReport groups and `/api/auth/calibration` byCategory bucket under `mlb|ml`. Calibration tab: `mlb-ml`.
- **MLB Spread / Run-line (v1, 2026-05-18)** — `KXMLBSPREAD` series. Each Kalshi market is "Team X wins by over Y runs?" with ticker suffix `{team}{N}` where `line = floor_strike = N − 0.5` (half-lines only — Kalshi MLB doesn't list integer lines). YES side bets margin team (`-line`); NO side bets cover team (`+line`) via independent `no_ask` (same UNDER pricing as totals — don't synthesize from `1 − yes_ask`). `spreadPctFromJoint(home, away, line, side)` reuses the joint Poisson draws from `_mlJointCache` (hoisted out of ML emission for sharing). All alt lines emit per Kalshi listing (typically ±1.5 / ±2.5 / ±3.5; sometimes more for big mismatches). `dcQualified + edge ≥ 5` gate, `DC_GATE("mlb","spread") = 8` (same as ML, same lambda inputs → same penalty table — `gameType === "spread"` added to the MLB ml branches in `api/lib/tonight/dc.js`). `pickLine` field is signed from the pick's perspective (`-1.5` for margin side, `+1.5` for cover side). Calibration bucket: `mlb|spread`, tab: `mlb-spread`. Frontend shipped 2026-05-18: PlaysColumn spread card (mirrors ML), MarketReport spread column, ModelPage calibration tab, client-side resolution via `App.jsx` (covers when `(pickFinal − oppFinal) + pickLine > 0`) and `liveStats.js` (in-game progress bar). AddPickModal does NOT support manual spread entry in v1.
- **NBA + WNBA Halves (v1, 2026-05-28)** — `KX{NBA,WNBA}{1H,2H}{TOTAL,SPREAD,WINNER}` series. Half lambdas derive from full-game λ × 0.5 captured in `_nbaMlContext` / `_wnbaMlContext` (`h1HomeLambda` / `h1AwayLambda` / `h2HomeLambda` / `h2AwayLambda` — all = full λ × 0.5 in v1, refined later if calibration shows Q4-pace/OT bias). Joint sim uses Normal with σ scaled by `sqrt(0.5)` (NBA: 9.2, WNBA: 7.8). 1H and 2H each get independent draws (`_halfJointCache` keyed by `${sport}|${homeTeam}|${awayTeam}|${half}`) — v1 simplification ignores full-game team-strength correlation between halves. Totals + spreads parse through the unified SERIES_CONFIG/parse loop (`segment: "1h"`/`"2h"` field). 3-way winner markets (`KX..1HWINNER`/`KX..2HWINNER`) fetched inline (snap → 600s legacy → REST mirror of KXMLBF5) — they include a `-TIE` suffix outcome priced ~3-5%. `joint3WayPct(home, away, side)` in `simulate.js` handles the home/away/tie reduction without dropping ties (renamed from `mlbF5MlPct` since it's sport-agnostic). Same DC gates as full-game (`total=10`, `spread=8`, `ml=8`) — segmented picks share the generalized `noSeasonSample` carve-out (`p.segment && p.segment !== "full"`). Synthesized half OU = full-game OU × 0.5 for threshold-distance anchor. Live resolution: `/api/live` NBA/WNBA branch reads ESPN's per-quarter `linescores` array — `h1Complete: true` when both teams have ≥2 entries (Q1+Q2 played); `h2Complete: true` at `state === "post"` (2H *includes OT*); h2 scores derived as `totalScore − h1Score`. `useLiveStats` resolution branch generalized from F5-only to handle any segmented pick. Frontend: PlaysColumn uses shared `segmentPillLabel` helper that returns "F5"/"1H"/"2H". Calibration buckets: `{nba,wnba}|h1{total,spread,ml}` and `h2*` equivalents (12 total). **Skipped in v1**: NBA quarters (Q1-Q4), NHL OT market (separate effort).
- **MLB First-5-Innings (v1, 2026-05-28)** — `KXMLBF5TOTAL`, `KXMLBF5SPREAD`, and `KXMLBF5` (3-way ML) series. Same parse + emit machinery as full-game total/spread but with F5-specific lambdas: `λ_F5 = teamRPG × 5/9 × oppStarterMult × park × platoon × weather × ump × lineupOutMult`. **Differences from full-game λ**: (1) NO TTO bump (3rd-time-through penalty starts past BF=22, i.e. inning 6+, so F5 ≈ 1st-time + part-of-2nd); (2) NO bullpen 40% share (F5 is starter-only — assumes the starter goes 5 IP); (3) NO regime blend (no per-game F5-runs data in `_gtScheduleMap`; revisit if calibration shows starter-driven λ misses). Days-rest, vs-L/R splits, park, platoon, weather, umpire, and lineup-out all still apply. **Joint sim**: `simulateMLBJoint(λ_home_F5, λ_away_F5, dispR, 10000)` reuses full-game dispersion `r` (acceptable v1 bias — F5 should be slightly less overdispersed, so reusing under-disperses tails marginally; revisit in calibration). One `_mlbF5JointCache` per game; total queries derive a combined-runs Int16Array from `home[i]+away[i]` for monotonicity. F5 spread reuses `spreadPctFromJoint` on the same joint draws. F5 ML uses `mlbF5MlPct(home, away, side)` which counts home-wins / away-wins / ties **without dropping** (TIE is a real Kalshi outcome, priced 15-17% typically). **Markets emit**: full alt-line set per Kalshi listing (typical F5 total thresholds 4–14, F5 spread half-lines ±1.5 / ±2.5, F5 ML emits all three sides per game — home/away/tie — independently gated by `dcQualified + edge ≥ 5`). **Same gates as full-game**: `DC_GATE("mlb","total")=10` for F5 total, `DC_GATE("mlb","spread")=8` for F5 spread, `DC_GATE("mlb","ml")=8` for F5 ML (no F5-specific gate — same penalty table since same trust signals; F5 ML tie pick reuses the same lambda inputs, so penalty table applies identically). **dc carve-out**: `noSeasonSample`/`tinySeasonSample`/`smallSeasonSample` skipped for F5 (no F5 seasonHitRate data). **Synthesized F5 OU line** = full-game OU × 5/9 so the threshold-distance penalty has an anchor; mlb buckets `[3, 5]` become effective `[1.7, 2.8]` for F5 — slightly tighter than ideal but workable v1. Calibration buckets: `mlb|f5total` / `mlb|f5spread` / `mlb|f5ml`, tabs: `mlb-f5total` / `mlb-f5spread` / `mlb-f5ml`. **Series fetch**: `KXMLBF5TOTAL` and `KXMLBF5SPREAD` go through SERIES_CONFIG/snap pipeline; `KXMLBF5` is fetched inline via snap → 600s legacy → REST (mirror of how `KXMLBGAME` is handled — it's a 3-way ML series, not a price-gate-filtered series). All three are in the kalshi.js snap cron list. Frontend: shares total/spread/ml PlaysColumn branches via `gameType` keying. ML card adds a `side === "tie"` branch (renders "Tie F5 ML (away @ home)" headline, both team logos stacked, no team-page navigation on click). All cards display an "F5" / "1H" / "2H" pill in the header via `segmentPillLabel` helper in PlaysColumn. Segment-aware `playKey`/`trackId` so segmented picks don't collide with full-game picks. **Live resolution (2026-05-28)**: `/api/live` MLB response stamps `f5HomeScore`/`f5AwayScore`/`f5Complete` once both teams have ≥5 entries in ESPN's per-half-inning `linescores` (i.e. bottom of 5 has been played to completion). F5 picks resolve mid-game on `f5Complete=true` — independent of full-game state — via a dedicated branch in `useLiveStats.js`. F5 total: OVER wins when `f5Home+f5Away ≥ threshold`. F5 spread: covers when `(pickF5 − oppF5) + pickLine > 0`. F5 ML: home/away resolve by F5 lead; tie pick wins on `f5Home === f5Away`. **Rainout/called-game handling**: if `state === "post"` AND `f5Complete !== true` (game ended before 5 full innings), F5 picks resolve to `result: "void"` (matches Kalshi settlement rules). Display caveat: `buildLiveProgress` still uses full-game running scores during innings 6+ (so a locked F5 pick may show full-game runs accumulating past the F5 threshold) — `result` field is authoritative, display is cosmetic. AddPickModal does NOT support manual F5 entry. **Open follow-ups**: (a) live mid-game resolution from per-inning linescore once bottom of 5 finishes, (b) extend `_gtScheduleMap` to capture per-game F5 runs so regime blend can be added, (c) `f5StarterShortLeash` dc penalty (-1 when `pitcherAvgBF < 18`) if calibration shows a low-BF subgroup bias.
- **NBA Moneyline (v1, 2026-05-20)** — `KXNBAGAME` series. Independent per-team Normal joint sim (`simulateNBAJoint(homeMean, awayMean, 13, 13, 10000)`) — Box-Muller draws clamped at 0, per-team σ=13 matches the NBA total sim variance. Same `mlPctFromJoint` (drops tied draws — rounded-Normal ties <0.5%, NBA OT resolves in reality so dropping is harmless). Lambdas captured into `_nbaMlContext` inside the NBA game-total loop where `_homeExpRaw`/`_awayExpRaw` are computed (line ~3568). Same `dcQualified + edge ≥ 5` gate as MLB ML, `DC_GATE("nba","ml") = 8`. NBA dc rules add: `nbaHomeHeavyInjury` / `nbaAwayHeavyInjury` -1, fires **once per game** for the heavier-injured side only when usage-out ≥ 0.30 (changed from two stacked penalties on 2026-05-26 — matches the WNBA shape for consistency). **No lineup-posted penalty** — NBA starting 5 isn't officially posted pre-game (ESPN `boxscore.players[].starter` only populates in/post), so penalizing the absence would gate every pre-game play. Lambda shrinks OffRtg by a piecewise-increasing function of total USG-out (since 2026-05-28; previously linear ×0.15 capped at 0.85, which under-shrunk multi-star scenarios — losing 49% USG only produced -7% adjustment). Tiers: ≤20% × 0.15, 20-35% × 0.22, 35-50% × 0.30, >50% × 0.35; floor cap 0.70. The heavy-injury dc penalty still fires at usage-out ≥ 0.30 as an additional gate. Calibration bucket: `nba|ml`, tab: `nba-ml`.
- **NBA Spread (v1, 2026-05-20)** — `KXNBASPREAD` series. Half-lines only (e.g. ±3.5 / ±6.5 / ±9.5 / ±12.5 / ±15.5 …; Kalshi never lists integer lines). Same ticker shape as MLB: suffix `{team}{N}` → `line = N − 0.5`. Reuses `spreadPctFromJoint` on `_nbaJointCache` draws shared with NBA ML emission. Same `dcQualified + edge ≥ 5` gate, `DC_GATE("nba","spread") = 8`. Same penalty table as NBA ML (same lambda inputs). `pickLine` signed from pick's perspective. Calibration bucket: `nba|spread`, tab: `nba-spread`. Frontend untouched — PlaysColumn / MarketReport / App.jsx resolution / liveStats progress bar already key on `gameType` not sport, so NBA spread plays render via the same branches as MLB.
- **WNBA Moneyline + Spread (v1, 2026-05-20)** — `KXWNBAGAME` / `KXWNBASPREAD`. Reuses `simulateNBAJoint` with per-team σ=11 (matches WNBA total sim). Lambdas captured into `_wnbaMlContext` from the WNBA branch of the game-total loop. Same gates as NBA. WNBA-specific dc rule: `wnbaHomeHeavyInjury` / `wnbaAwayHeavyInjury` -1, fires **once per game** for the heavier-injured side only when usage-out ≥ 0.30 (changed from two stacked penalties on 2026-05-26 — WNBA roster size makes the both-sides case common and was capping most ML/spread plays at dc=8). **No lineup-posted penalty** (same reason as NBA — WNBA starters not posted pre-game). Calibration buckets: `wnba|ml`, `wnba|spread`; tabs: `wnba-ml`, `wnba-spread`.
- **NHL Moneyline + Spread (v1, 2026-05-20; NegBin upgrade 2026-05-29)** — `KXNHLGAME` / `KXNHLSPREAD`. Reuses `simulateMLBJoint` (per-team NegBin since 2026-05-29 — same engine as MLB). `_fitNhlDispersion()` fits dispersion `r` from per-team NHL schedule residuals (fallback r=6); stored as `_nhlMlContext[key].dispR` and passed to `simulateMLBJoint` for ML + spread. `simulateNHLTotalDist` also upgraded to NegBin (same r); NHL total seasonHitRate blend updated to `muForNegBinTail`/`negBinCDF`. **Spread dampener (2026-05-29):** `yesTruePct = 0.80 × sim + 0.20 × 65` — safety net while calibration builds (n=6, 50% actual vs 83% model pre-fix); tune or remove once n≥20. **OT/SO approximation:** regulation-only — ~22% of NHL games go to OT/SO, treated as dropped ties in ML numerator. DC rules extend `homeGoalieTeamFallback`/`awayGoalieTeamFallback` to fire on `ml` and `spread`. No lineup penalty. Calibration buckets: `nhl|ml`, `nhl|spread`; tabs: `nhl-ml`, `nhl-spread`.

---

## Key Gotchas

**NBA/WNBA injury OffRtg shrink — piecewise + blend damp (2026-05-28)**: Two-part fix shipped same day.

(a) `_injuryOffRtgAdj` in `api/lib/handlers/tonight.js` (~line 1440) was previously linear `1 - share/100 × 0.15` floored at 0.85. With 49% USG out (e.g. OKC playoff game missing SGA + Chet + role player) that only produced -7% adj — flat-out wrong because losing a primary creator collapses motion offense non-linearly. Now piecewise: ≤20% × 0.15, 20-35% × 0.22, 35-50% × 0.30, >50% × 0.35, floor cap 0.70.

(b) On the OKC test case, the piecewise fix correctly shrunk modelTruePct to 79.4% — but `ttSeasonHitRate` blend and `regimeBlendW` (both anchored to games where the now-injured players DID play) pulled final truePct back up to 88.7%. Follow-up adds `_injuryBlendDamp(usageOut)` = `max(0, min(1, (60 - usageOut) / 30))` that scales both blends linearly to 0 between 30-60% USG out. Applied per-side (home/away or scoring-team) at: NBA game total regime blend, WNBA game total regime blend, NBA team total regime blend, NBA team total `ttNbaSeasonHitRate` blend (`_w *= _ttInjDamp`). Emits new fields `homeInjDamp` / `awayInjDamp` (game total) / `scoringInjDamp` (team total) when damping is active.

Applies to NBA + WNBA game totals + team totals (shared helper), inherits into NBA/WNBA ML + spread via `_nbaMlContext` / `_wnbaMlContext`. 30% threshold matches existing `nbaHeavyInjury` / `wnbaHeavyInjury` dc rule. Filter `trackedAt < 2026-05-28` from NBA/WNBA total/teamTotal/ml/spread calibration.

**Regime-aware lambda blend (2026-05-21, MLB game totals added 2026-05-25, MLB team totals added 2026-05-27)**: NBA / WNBA / NHL game totals + NBA team totals + MLB game totals + MLB team totals all blend each team's recency-weighted recent-score average into the rating-based expected. Exponential decay; 21-day half-life for NBA/WNBA/NHL, 14-day for MLB (`_SEASON_HALF_LIFE_DAYS` / `_MLB_HALF_LIFE_DAYS`). Shared blend weight `_regimeBlendWeight = min(0.85, sample/8 * 0.85)` for game totals + NBA team totals; **MLB team totals use a dedicated conservative inline cap of 0.50 / denom 12** because single-team variance > two-team-combined. Replaces the prior fixed 4% NBA playoff boost. Helpers `_recencyWeight` + `_recentTeamScoreMean(schedMap, sport, team, halfLife?)` are hoisted to shared scope so both the game-total block (uses `_gtScheduleMap`) and the team-total block (uses `_ttScheduleMap`) can call them. Filter `trackedAt < 2026-05-21` from NBA/WNBA/NHL total + NBA teamTotal calibration; `trackedAt < 2026-05-25` from MLB total/ML/spread; `trackedAt < 2026-05-27` from MLB teamTotal. Emitted fields on play: `regimeBlendW`, `homeRecentMean`/`awayRecentMean` (or `scoringRecentMean` for team totals).

**MLB lineup-aware lambda (2026-05-18)**: MLB game-total / team-total / ML / spread lambdas now incorporate (a) a tiered lineup multiplier per side based on `sportByteam.mlb.injuryByTeam[team]` (excluding pitchers via `pos` field + pitcher-ID match) — 0/1/2/3+ hitter-outs → λ × 1.0/0.98/0.96/0.93; and (b) probable-pitcher IL detection (ID match on `pitcherInfoByTeam[team].id` vs injury report) that nulls the starter inputs so the existing fallback uses bullpen-only ERA. `buildMlbInjuryReport` in `api/lib/mlb.js` (ESPN endpoint, `mlb:injuries:{date}` 30min cache, CHW→CWS normalization). dataConfidence penalties: `-1` per side at `topOut ≥ 2`, `-2` per side at `pitcherOnIL`. Player props are NOT affected (HRR/K already use lineupSpotByName for per-batter signals). Filter `trackedAt < 2026-05-18` from calibration when comparing total/teamTotal/ML/spread lambdas across this change.

**TEAM_NORM (Kalshi → ESPN)**: NBA `{ GS→GSW, SA→SAS, NY→NYK, NJ→BKN, NO→NOP, PHO→PHX, WPH→PHX, KAT→ATL }`. WNBA `{ CONNECTICU→CONN, CON→CONN, DALLAS→DAL, WAS→WSH, GSV→GS, LAS→LA }` — ESPN returns Connecticut as either `CON` (current byteam) or `CONNECTICU` (older scoreboard); both alias to canonical `CONN`. Kalshi tickers use canonical (`CONN`, `DAL`). After building `STAT_SOFT["nba|*"]` / `STAT_SOFT["wnba|*"]` rankMaps from ESPN byteam (which also returns short codes), a post-normalization loop adds the long-form key so `nbaDefRank["GSW"]` resolves.

**WNBA `parseGameTeams` — variable-length abbrs**: WNBA mixes 2-, 3-, and 4-char canonical abbrs (`LV/NY/GS/LA` + `ATL/IND/DAL` + `CONN`). NBA's 3+3-first / 2+3-fallback parser doesn't handle 2+2 (`LVNY`, `GSLA`) or 4-char halves (`CONNIN`, `LVCONN`). The WNBA branch in `parseGameTeams` tries every (i, len−i) split with both halves length 2–4, preferring longer left-side first (so `CONNIN` → `CONN+IND`, not `CO+NNIN`). `_VALID_TEAMS["wnba"]` is the 15-team canonical set; tickers also use this set, so the longest-prefix-wins heuristic is safe.

**`parseGameTeams` validation via `_VALID_TEAMS`**: The Kalshi event ticker's team segment is parsed by trying splits in order. Without validation, a 2-char Kalshi prefix that's *also* the first 2 chars of the actual 3-char team would steal the parse — e.g. `NYKPHI` would split as `NY`(→NYK) + `KPH` (garbage), turning a PHI vs NYK matchup into NYK vs KPH. Same for `SASMIN`→`SA`+`SMI`. Symptom: duplicate matchup cards on the home page (one keyed by the Kalshi-corrupted abbrs, one by ESPN's correct gameScores entry). Fix: hardcoded `_VALID_TEAMS` set per sport — the parser tries 3+3 first when length≥6 and only commits if both halves validate; falls back to 2+3 (also validated) only if 3+3 fails. Affected pairings: NBA SAS/SAC/NYK/NOP, NHL NJD/TBL/LAK/SJS, MLB ARI (via "AZ" prefix). Maintain `_VALID_TEAMS` when teams rebrand (e.g. OAK→ATH).

**Kalshi ticker home/away order doesn't match ESPN.** `parseGameTeams` returns `[team1, team2]` in ticker order, which is *not* necessarily home/away. Each play loop's homeTeam/awayTeam assignment must look up the actual home team and swap if needed: MLB uses `sportByteam.mlb.gameHomeTeams[gameTeam2]`; NBA/NHL scan `sportByteam.{nba,nhl}GameScores` values for a matching `homeTeam == gameTeam2 AND awayTeam == gameTeam1` pair. Without this, matchup cards display the inverted "AWAY @ HOME" string for any game where Kalshi's ticker order disagrees with ESPN's home assignment. Two sites enforce this: the game-total loop and the team-total loop (NBA team total is the only non-MLB team total in production today; NHL has no team totals).

**NHL_ABBR_MAP**: NHL Stats API teamIds → abbreviations. **UTA (Utah Mammoth) = teamId 68** (rebranded from Utah Hockey Club for 2025-26; old teamId 53 absent). New teams showing `—` for GPG/GAA/SA need their teamId added.

**ESPN scoreboard abbr mismatch**: ESPN scoreboard's `team.abbreviation` differs from our canonical for several teams. `/api/live` translates at the ESPN boundary via `CANONICAL_TO_ESPN` / `ESPN_TO_CANONICAL` (sport-keyed) so picks tracked with the canonical abbr still match the ESPN event, and the response's `homeTeam`/`awayTeam` come back canonical (matching `pick.homeTeam` etc.). Symptom of an unmapped team: `/api/live` returns `state:"unknown"` and the pick never auto-resolves. Current map:
- **MLB**: `CWS↔CHW` (Chicago White Sox; canonical from `MLB_ID_TO_ABBR[145]`)
- **NBA**: `GSW↔GS`, `SAS↔SA`, `NYK↔NY`, `NOP↔NO`, `UTA↔UTAH`, `WAS↔WSH`
- **WNBA**: `CONN↔CON` (live scoreboard returns short `CON`; older byteam returns `CONNECTICU`; `DAL` matches canonical on the scoreboard so no `/api/live` translation needed)
- **NHL**: `TBL↔TB`, `NJD↔NJ`, `LAK↔LA`, `SJS↔SJ`

If a team rebrands or a new mismatch surfaces, add it to `CANONICAL_TO_ESPN` in the `/api/live` handler — `ESPN_TO_CANONICAL` is auto-derived. Same canonical-vs-ESPN-abbr divergence applies when filtering ESPN-sourced schedule events — see `docs/MODEL.md` for `_gtScheduleMap`/`_ttScheduleMap` `normTeam` requirement.

**gameTimes lookup chain** (in play loop): `sport:team:gameDate` → `sport:team:tomorrowISOStr` (handles Kalshi encoding tomorrow's games under today's ticker date) → bare `sport:team`.

**`gameScores` today + tomorrow merge**: Each ESPN scoreboard fetch that produces `gameScores` (MLB tonight, NBA tonight, NBA fallback, NHL fallback) fetches **today AND tomorrow in parallel** (PT date, `Date.now() - 7h`) and passes merged events to `parseGameScores`. Today's events alone go to `parseGameOdds`/probables (so tomorrow doesn't overwrite today's pitcher/odds). `parseGameScores` keys by `${hA}|${gameDate}|${event.date}` so (a) today's NYY and tomorrow's NYY don't collide and (b) same-day doubleheaders (e.g. DET @ BAL twice) don't either. Without (a), when today's MLB is all `state==="post"` (or after midnight UTC for NBA/NHL using UTC date), today's "Final" data is wiped. Without (b), the second game of a doubleheader overwrites the first and only one matchup card renders. Inline duplicate in `api/lib/mlb.js` (around line 952) must mirror the same key shape. Frontend mirror: `LineupsPage.buildGames` keys by `${sortedPair}|${gameDate}|${gameTime}` for the same reason — plays carry the upstream `gameTimes[sport:team:date]` value (latest-of-day wins, line ~832), so DH plays attach to game 2's card; game 1's card stands alone with Final + scores from gameScores.

**`/api/live` doubleheader disambiguation**: Wire format extended to `sport:t1:t2@gameTimeISO` (optional `@gameTime` suffix). Client `buildLiveGameKeyRaw` (`src/lib/liveStats.js`) appends `@${pick.gameTime}` when present; the App.jsx live-poll loop mirrors the same shape. Server (`api/lib/handlers/sports.js`) splits on `@`, filters scoreboard events by `ev.date.slice(0,16) === gameTime.slice(0,16)` (tolerates ESPN's no-seconds format vs MLB Stats API's with-seconds form) so the right DH game is matched. Cache key includes gameTime so game-1/game-2 boxscores don't collide. No fallback when gameTime is supplied but doesn't match — falling back would reintroduce the bug where a pick on game 2 settles against game 1's Final. Existing tracked picks settled before this fix stay settled; future polls won't re-resolve already-`result`-stamped picks.

**`_mlbMlContext` null-gameDate fallback (2026-05-29)**: KXMLBTOTAL tickers routinely have unparseable date segments — on 2026-05-29 all 15 games on the slate had `gameDate: null` in `totalMarkets`, so this is a systemic pattern, not a one-off edge case. When null, the full-game context is stored under `_mlbMlContext["HOME|AWAY|null"]`. The F5 total/spread market objects derive their own `gameDate` from the ticker or `expected_expiration_time` in PT, which may differ (e.g. "2026-05-30" for a late game that expires past UTC midnight). Context lookups in the F5 total, F5 spread, full-game spread, F5 ML, and full-game ML emit blocks would fail since `"HOME|AWAY|2026-05-30" !== "HOME|AWAY|null"` — no context found, no plays generated, game silently absent from market report. Fix: all context lookups chain a `?? _mlbMlContext["t1|t2|null"] ?? _mlbMlContext["t2|t1|null"]` fallback; ML/F5-ML (which iterate contexts and look up markets) chain a `?? market["home|away|_todayPT"]` fallback when `ctx.gameDate == null`. `_mlbBothTeamsConfirmed` is called with `ctx.gameDate ?? _todayPT` (not the market's potentially wrong `gameDate`) so lineup confirmation is evaluated against the correct day. Symptom if broken: F5/spread/ML plays entirely absent for some or all games in the market report; totalRuns plays present but with `gameDate: null`.

**Kalshi MLB postponed-ticker reattribution**: When an MLB game is rained out, Kalshi keeps the original ticker (date segment + `expected_expiration_time` both stale) and reuses it for the make-up game. The market-parse loop in `/api/tonight` stamps `gameDate` from the ticker date segment, so a postponed-but-played-today market would otherwise get filtered out as "yesterday's game" and never reach the matchup card. After `sportByteam.mlb` is hydrated, the handler builds `_mlbNextGameByTeams[sortedPair] = {gameDate, gameTime}` from `sportByteam.mlb.gameScores` (only `state === 'pre'` entries with `gameDate >= todayPT`), then `_reattrMlbGameDate` overwrites the stale gameDate on `totalMarkets`/`teamTotalMarkets`/`spreadMarkets` and on the ML/total Kalshi parse branches (`_byEventMl` aggregation around line 4419, KXMLBGAME and KXMLBTOTAL parse for the gameOdds Kalshi fill around lines 5286/5320). Resolved markets are detected via extreme prices (`yes_ask`/`no_ask` pinned at 0.01/0.99/1.00, or for totals no threshold pct in [5,95]) and SKIPPED — that keeps yesterday's actually-played-yesterday settled prices from being misattributed to today's game. Example bite: 2026-05-24 DET @ BAL game 2 (3 PM PT make-up) traded as `KXMLBTEAMTOTAL-26MAY231605DETBAL-*` because the original May 23 game was rained out; without reattribution the entire ticker block silently disappears from /api/tonight.

**MLB pitchers per-game (`mlbMeta.pitchersByGame`)**: Keyed by `${team}|${gameKey}` where gameKey is the ESPN-style ISO (no seconds, `2026-05-24T16:35Z`). Built from `pitcherIdByGame` (populated in `buildPitcherKPct`'s schedule loop using MLB Stats API's per-game `gameDate` trimmed of seconds via `_trimSec`) → joined to per-id stats (`pitcherEraById` / `pitcherWinsById` / `pitcherLossesById` / `pitcherNameById`). `mlbMetaTomorrow.pitchersByGame` built the same way during the tomorrow-probables loop. `MatchupCard.featureFor` reads `pitchersByGame[\`${abbr}|${gameTime}\`]` first, falls back to the per-team `pitchers[abbr]` map. Without the per-game map both DH cards would show the late game's pitcher (per-team `pitcherInfoByTeam[BAL]` only holds one entry). Per-team caches that still collapse DH games (`gameOdds[BAL]`, `lineupKPct[BAL]`, `pitchers[BAL]`) are acceptable: odds are typically the same across DH games, and lineup is suppressed for in/post games in `MatchupCard.mlbLineupStatus`.

**Two-way players** (MLB strikeouts): ESPN gamelog defaults to **batting** stats. The play loop appends `&category=pitching` for all MLB K-market players and pitcher gamelog fetches. Separate Redis cache keys (`gl:mlb242526pv1`, `gl:mlb2025p|`, `gl:mlb2026p|`) prevent batting/pitching collision. Without this, two-way players (e.g. Ohtani) drop with `col_not_found` because the K column is absent from batting gamelog.

**ESPN gamelog endpoint**: ESPN now blocks server-side HTML page fetches with AWS WAF. Use the JSON API (`site.web.api.espn.com/apis/common/v3/sports/{sport}/{league}/athletes/{id}/gamelog`) for ALL sports including NBA/NHL.

**NBA lineup source chain**: (1) ESPN scoreboard → game summary boxscore starters (today's actual, `lineupConfirmed:true`); (2) most recent **playoff** schedule game first (`seasontype=3`), fallback to regular season `lastGameId` only if no playoff games — boxscore starters; (3) ESPN team roster (one player per position group, up to 8). ESPN depth chart (`/teams/{abbr}/depthchart`) returns `{}` during playoffs — removed. Always prefer playoff over regular season — RS finals often have rested/bench starters that don't reflect playoff rotations.

**MLB lineup** (TeamPage `/api/team`): (1) today's MLB Stats API schedule `hydrate=lineups,probables` posted batting order → `lineupConfirmed:true` with `spot:1-9`; (2) most-recent posted lineup from the team's past-10-day schedule (`/schedule?startDate=...&endDate=...&hydrate=lineups`, walk newest→oldest) → `lineupConfirmed:false` with `spot:1-9`, shown as "Expected batting order — based on most-recent posted lineup"; (3) active roster fallback (non-pitchers, up to 12, `spot:null`, `lineupConfirmed:false`). Probable-pitcher entry (from path 1's same fetch) is preserved across (2) so even a fallback render shows tonight's starter.

**`mlbMeta.pitchers[abbr]` shape**: `{ name, id, era, wins, losses }`. `id` is the MLB Stats API personId (used for `midfield.mlbstatic.com/v1/people/{id}/spots/120` headshots — preferred over ESPN). Sources are merged in `api/[...path].js` ~line 4890: `name` from probables→pitcherInfoByTeam; `id` from pitcherInfoByTeam→probables; `era` from `pitcherEra` (MLB Stats API)→probables; `wins`/`losses` from `pitcherWinsByTeam`/`pitcherLossesByTeam` (2026 if any starts/decisions, else 2025). **Note (2026-05-18)**: `pitcherEra` is the two-step-regressed value (`_regressedRate` in `api/lib/mlb.js`, see docs/MODEL.md), not raw season ERA — same value the lambda math uses. The `probables` fallback (from MLB Stats API schedule hydrate) is still raw season ERA, so a pitcher with no entry in `pitcherEra` (no 2026/2025 starts) will display raw. `mlbMetaTomorrow.pitchers` parallel-fetches the `personIds=...&hydrate=stats(group=pitching,type=season,season=2026)` endpoint once per `_tmrDateStr` (cache key `mlbTmrPitcherStats:{date}`, 10min TTL) to populate ERA/W-L for next-day probables. Any field may be null; `MatchupCard` only renders the pieces present.

**Closing-line snapshots (`mlbClosingOdds` / `nbaClosingOdds` / `wnbaClosingOdds` / `nhlClosingOdds`)**: ESPN scoreboard returns empty `odds` once a game is in/post, so live odds maps would lose entries mid-day. One Redis key per sport holds `{"home|away|gameDate": {home, away}}`. NBA/WNBA/NHL share an inline `_applyClosingSnapshot(snapKey, scoresMap, oddsMap)` helper; MLB uses a parallel block (it must run *before* the Kalshi fill, and after `_mlbGameOddsTomorrow` is computed). For each game in `gameScores`: `state==='pre'` writes the live odds to the snapshot (live odds map chosen by gameDate vs today's PT — MLB today's games use `_mlbGameOdds`, tomorrow's use `_mlbGameOddsTomorrow`); `state==='in'|'post'` overlays the snapshot back so the matchup card displays the *closing* line. 36h TTL refreshes on each write; keys aren't date-prefixed so they survive midnight rollover. **If no snapshot exists for an in/post game** (e.g., the service was first deployed mid-game), live odds entries are **cleared** rather than left visible — in-progress prices look like real lines but are model-derived mid-game values, so we hide them rather than mislead.

**`mlbMeta.gameOdds` vs `mlbMetaTomorrow.gameOdds`**: Today's odds come from `parseGameOdds(sbData.events)` (today-only ESPN scoreboard fetch); tomorrow's from `parseGameOdds(sbData.eventsTomorrow)` (also fetched in the same `Promise.all` at the byteam:mlb build site). Both are normalized through `MLB_ESPN_NORM` and persisted on `sportByteam.mlb` as `gameOdds`/`gameOddsTomorrow`. `MatchupCard` selects between today and tomorrow by `game.gameDate` vs PT today so a team playing on both days doesn't show today's line on the tomorrow tab.

**Kalshi-derived MLB odds fallback**: ESPN doesn't post tomorrow's lines until close to first pitch. Right before the closing-odds snapshot, we walk the already-fetched `KXMLBGAME` and `KXMLBTOTAL` market arrays. For ML: take both teams' yes-prices, normalize to remove overround, convert to American odds. For total: same "highest threshold where yes≥50%" heuristic used elsewhere. Keyed by `{abbrA}|{abbrB}|{gameDate}` (gameDate parsed from `expected_expiration_time` → PT) and only fills *missing* fields on `_mlbGameOdds` / `_mlbGameOddsTomorrow`. ESPN remains authoritative when it has data. The Kalshi fill runs before the snapshot block, so pre-game writes to `mlbClosingOdds` capture whichever source supplied the line, and in/post overlays still display the closing snapshot rather than live Kalshi.

**`{sport}Meta.topPlayers[abbr]` (NBA/WNBA/NHL)**: `{ name, id, headshot, stats }` extracted from ESPN scoreboard `competitions[].competitors[].leaders` by `parseTopPlayers` in `api/lib/utils.js`. NBA/WNBA use the `RAT` (Rating) category leader and parse `displayValue` (`"X PPG, Y RPG, Z APG, …"`) into `"X PPG · Y RPG · Z APG"`. NHL uses the `Points` leader; derives the missing leg whichever way it can — if the same athlete also leads Goals, computes `A = P − G`; if they also lead Assists, computes `G = P − A`; otherwise falls back to just `"PTS"`. Headshot prefers `athlete.headshot.href`, falls back to `https://a.espncdn.com/i/headshots/{league}/players/full/{id}.png`. Today-only — `MatchupCard` looks up by raw `homeTeam`/`awayTeam` abbr without a date-based selector (NBA/NHL/WNBA tabs are typically single-day).

**byteam:mlb partial-cache trap**: MLB byteam hydrates several MLB Stats API calls in parallel and each `.catch(() => ({}))` silently. A successful lineup fetch alongside a failed OPS or gamelog fetch would otherwise bake partial data for 10min — short-TTL guard (60s) fires when any of `lineupSpotByName`, `pitcherAvgPitches`, `hitterOpsMap`, `pitcherH2HStarts` is empty. Symptom: ReportPage Market tab columns null across many rows; diff cached vs `?bust=1` to confirm.

**Edge handler env-var wiring**: ALL env vars must be passed through `process.env` to the explicit `env` object at the bottom of `api/[...path].js`. Vercel doesn't auto-attach them. If you add a new env var, add it here too:
```js
const env = {
  UPSTASH_REDIS_REST_URL: process.env.UPSTASH_REDIS_REST_URL,
  UPSTASH_REDIS_REST_TOKEN: process.env.UPSTASH_REDIS_REST_TOKEN,
  JWT_SECRET: process.env.JWT_SECRET,
  ADMIN_KEY: process.env.ADMIN_KEY,
  CRON_SECRET: process.env.CRON_SECRET,
  KALSHI_API_KEY_ID: process.env.KALSHI_API_KEY_ID,
  KALSHI_PRIVATE_KEY: process.env.KALSHI_PRIVATE_KEY,
};
```
Symptom of missing wire-up: `env?.VAR` is `undefined` even though Vercel dashboard shows it set. JWT_SECRET specifically: `TextEncoder.encode(undefined)` = 0 bytes → `"Imported HMAC key length (0)"` 500 on login.

**Model version (v1 fully dropped 2026-05-26)**: Universal client qualification is `dcQualified === true && edge >= 5`. The previous v1 SimScore gate (`finalSimScore >= 8` etc.) was deprecated 2026-05-18; the v1-specific server emit branches and client `qualified !== false` filters were removed 2026-05-26. SimScore is computed and emitted for display/attribution only. **Behavior change**: plays with SimScore < 8 now qualify if `dcQualified + edge ≥ 5`. Filter `trackedAt < 2026-05-26` from calibration when comparing pre/post-cleanup pick volumes.

- Client `EDGE_GATE = 5` (App.jsx, LineupsPage.jsx). Server `EDGE_GATE = 3` for calibration continuity — every pick is stored at edge≥3, client filters to ≥5.
- `trackPlay` still stamps every new pick `modelVersion: "v2"` so `/api/auth/calibration` can keep splitting historical v1 picks vs current v2 picks.
- The `qualified` field is still emitted by the server but only carries an alt-line-demotion semantic (winning alt-line = `qualified: true`, demoted = `qualified: false`) and the MLB-K per-threshold extras for the player card. It no longer reflects SimScore.
- `/api/auth/calibration` retains `?modelVersion=v1|v2|all` filter + `byModelVersion: { v1, v2 }` breakdown for historical comparison.

**Kalshi UNDER pricing — use `no_ask_dollars`, not `1 - yes_ask_dollars`**: Kalshi binary markets have **independent** YES and NO order books. `yes_ask` is the cost to buy YES; `no_ask` is the cost to buy NO. They are NOT mirrors — typical spread is 3–7 cents (e.g. yes_ask=0.17, no_ask=0.87 instead of the "fair" 0.83). Synthesizing the UNDER price as `1 - yes_ask` systematically underprices UNDERs and inflates their measured edge by 3–7%. **Fix landed 2026-05-15**: the parse loop now reads `m.no_ask_dollars` alongside `yes_ask_dollars` and propagates `noKalshiPct` + `noKalshiAO` through `totalMarkets`/`teamTotalMarkets` push payloads. Emission sites (game total ~4684, team total MLB ~4882, team total NBA ~4969) consume the passed-through fields with a `?? (100 - kalshiPct)` fallback for the missing-data case. Player-prop UNDERs are not affected (Kalshi doesn't list player-prop UNDER alt lines as separate markets). Pre-fix UNDER picks tracked before 2026-05-15 have wrong `kalshiPct`/`americanOdds`/`edge` values — filter them out of calibration.

**Kalshi snap-first read chain (`/api/tonight`)**: Layered fallback so a cron-down or 429-spiked Kalshi doesn't blank the page:
1. **`kalshi:snap:{ticker}`** — written every 2 min by `/api/kalshi-snapshot` cron. All-or-nothing: if every series snap is fresh (`writtenAt` within 180s, i.e. 1.5 cron cycles) we use them and skip Kalshi REST entirely. Read via single Upstash MGET so each `/api/tonight` invocation is 1 Upstash command for snap data + 1 for meta.
2. **`kalshi:bundle:{date}`** — legacy 600s bundle. Used if snap-first fails (cron down, partial snap miss).
3. **REST + `kalshi:stale:{ticker}`** — existing per-ticker fetch with throttled batches and 30-min stale fallback. Kicks in for cold-start and full-snap-miss cases.

`KXMLBGAME` (MLB game ML, not in `SERIES_CONFIG` but used for tomorrow's odds fallback) has the same 3-tier chain: `kalshi:snap:KXMLBGAME` → `kalshi:KXMLBGAME` (600s legacy) → direct REST.

The all-or-nothing snap gate is intentional. Mixed-source recovery (some snaps fresh, some REST) adds complexity without meaningfully reducing failure modes — the bundle/REST fallback already handles a partial snap miss correctly.
